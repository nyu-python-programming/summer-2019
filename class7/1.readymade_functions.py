"""
Functions that exist already and you do not have to make yourself

@author Foo Barstein
@date 6 June 2019
"""

# there do exist some "built-in" functions in the python language
# see the full list : https://docs.python.org/3/library/functions.html
print("Hello world!") #the print function
int("5") # the int function!
bool("True") # the bool function!

# there are other functions that are tied to particular data types
"foo {}".format("bar")
"foobar".find("oob")
"FOOBAR".lower()
"FOOBAR".isupper()
"12345".isnumeric()

# there are additional functions that exist within modules/libraries/apis
import random
random.randint(1, 10) 

